import 'package:flutter/material.dart';

class AppColors {
  static const menuBar = Color(0xFF263238);
  static const toolBar = Color(0xFF37474F);
  static const canvas = Color(0xFFECEFF1);
  static const panel = Color(0xFFCFD8DC);
  static const panelHover = Color(0xFFB0BEC5);
  static const icon = Colors.white70;
  static const accent = Colors.amberAccent;   // активний інструмент
  static const onBarMuted = Colors.white70;
  static const selection = Colors.blue;        // рамка й опорні ручки
  static const rotationHandle = Colors.green;
  static const danger = Colors.red;            // «Видалити»
}

class AppSizes {
  static const barHeight = 48.0;
  static const toolbarHeight = 64.0;
  static const panelToggleWidth = 22.0;
  static const handleRadius = 6.0;
  static const space = 8.0;
}